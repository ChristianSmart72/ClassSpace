{
  "name": "classspace",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npm run build",
    "start": "NODE_ENV=production node server/dist/index.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "nanoid": "^6.0.0"
  },
  "engines": {
    "node": ">=22 <25"
  }
}
